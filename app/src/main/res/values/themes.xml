<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style
        name="CustomToolbarStyle" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:textColorPrimary" tools:targetApi="l">#FFFFFF</item>
        <!-- Customize your theme here. -->
        <item name="android:colorControlNormal" tools:targetApi="l">#FFFFFF</item>
    </style>
</resources>